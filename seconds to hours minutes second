#include <stdio.h>

int main()
{
    int s, h, m, r;
    scanf("%d", &s);

    h = s / 3600;
    r = s % 3600;

    m = r / 60;
    r = r % 60;

    printf("hours=%d minutes=%d seconds=%d", h, m, r);

    return 0;
}
